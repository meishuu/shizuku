exports.module = function(){ return new SAMPLE(this); };

var SAMPLE = function($){
	$.META = {
		author: 'AUTHOR NAME',
		version: '0.1',
	};
	$.on('onPrivmsg', this.onPrivmsg);
};

SAMPLE.prototype = {
	onPrivmsg: function(from, to, msg){
		// do something here
	},
};
